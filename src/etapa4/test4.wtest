import pepita.*
import comidas.*
import masAves.*
import roque.*

test "roque trabaja" {
    roque.tuPupiloEs(pepita)
    roque.entrenar()
    roque.tuPupiloEs(pipa)
    roque.entrenar()
    assert.equals(1165, pepita.energia())
}

test "roque trabaja1" {
    roque.tuPupiloEs(pepita)
    roque.entrenar()
    roque.entrenar()
}
//¿se rompe algo? y ¿cómo queda afectada Pepita?
//el test debe tener algo para comparar, no se rompe 